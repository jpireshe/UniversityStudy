#include<stdio.h>
#include<stdlib.h>
int init_littlelib();
int randint(int lower, int upper);
int is_prime(int n);
int arraylen(int a[]);
int sumnums(int a[]);
void printarray(int a[]);
void shuffle(int *a, int len);
